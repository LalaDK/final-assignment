development:
  clients:
    default:
      database: final_assignment_development
      hosts:
        - localhost:27017
      options:
  options:
test:
  clients:
    default:
      database: final_assignment_development
      hosts:
        - localhost:27017
      options:
        read:
          mode: :primary
        max_pool_size: 1
